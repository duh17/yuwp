# Pre-results scope lock: dev-only non-acceptance diagnostics

This addendum supersedes the execution authorization in protocol.md. No candidate output existed when written. The parent and independent review identified missing representative heldout formatting/spontaneous gold, and an infeasible controlled schedule (clearance plus playback alone exceeds four hours). **Heldout execution is not authorized.** Keep all 40 heldout speech clips and six heldout diagnostics untouched by inference. Do not implement production support.

The immediate deliverable is a measured, bounded runtime/quality diagnostic, not a confirmation run. No universal English claim is intended. Future qualification would need a separately frozen representative corpus with human spontaneous and punctuation/case/number formatting gold, strict unchanged numerical margins, and feasible power/schedule. Existing AMI dev-only excerpts cannot fill that heldout gap. Do not expand data after seeing candidate output in this experiment.

## Allowed execution

Attempt load clearance for at most 20 minutes after setup completes, without stopping others. If it fails, record `blocked-load-control`. Execute this fallback only with the explicit `--load-status diagnostic-under-load` flag, which retains `no_load_control=true` and the NON-ACCEPTANCE label. Never use `external-clearance` to bypass a blocked check. Run one warm dev pass only: Qwen + N1 + P1, 12 dev speech clips plus the 15 diagnostics whose `split` is dev, at most 81 scored trials. No N2/P2, process-cold phase, retries of measured trials, heldout, or acceptance inference. Retain all failures. Maximum four wall-clock hours including warm-ups and process startup. On timeout, finish incomplete; do not discard slow cases or refill missing pairs.

Even if load becomes quiet, this authorized run remains **NON-ACCEPTANCE** because coverage and confirmation approval are absent. Every result records this label and its observed load status. No parity confidence interval, inference of candidate-caused latency, or promotion from these observations. Report paired descriptive final WER and p50/p95 first-nonempty, first-useful, stable-useful, and stop-to-final measurements; undefined useful-prefix endpoints remain missing, not zero. First nonempty text measures visibility separately from correct-prefix quality. Clear dev quality failures can reject a candidate; apparently favorable results are not confirmation. Headline speech metrics must be stratified into the 12 LibriSpeech dev clips and nine AMI spontaneous dev clips. Synthetic diagnostics are a separate stratum; silence/noise/cutoff latencies must not be pooled into headline speech results. Transformed repeats of one source clip are not independent samples.

Use protocol.md's fixed serial per-clip system permutations, warm-up, packet-end real-time deadlines, framing, stop-intent clock, asset/normalizer pins and invariants. Exclude cutoff (gold unavailable), silence-only and noise-only from lexical WER. Score silence/noise hallucinations and cutoff lifecycle separately. All 15 dev diagnostics remain in the run even when not WER-scored. At most one measured trial per system/case; no measured trial starts until build/download tasks are complete and no other ASR benchmark is active.

N1: pinned 560ms Nemotron, fused decoder enabled when its pinned bundle exists, CPU+ANE model configuration, clean manager recreated for each stream, active text until final. P1: pinned v2, CPU-only preprocessor/decoder/joint according to SDK loader, CPU+ANE encoder, full-prefix previews at 1s and each additional 1s of received samples, 45s hard cap, final full-input pass; active text until final. No claim of immutable pre-stop commits. Record SDK-resolved compute units and loading health probe separately; that probe makes Nemotron process-cold inference untestable here.

## Pre-run sign-off remains required

Independent protocol review approved this fallback in principle with finite corrections. Before measured output: review the exact adapter/evaluator source, test framing/clock/response/error behavior, and record binaries, assets, commands, live-VAD readiness and all protocol hashes. A compile success alone does not satisfy this. This diagnostic scope does not authorize landing; the parent owns independent verification and any ff-only integration.
